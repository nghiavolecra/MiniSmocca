<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Smocca Clone Demo</title>
  <!-- Thêm Bootstrap CSS từ CDN -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <style>
    /* Custom CSS (bạn có thể điều chỉnh lại theo nhu cầu) */
    body {
      font-family: Arial, sans-serif;
      background-color: #f7f7f7;
    }
    .navbar-brand img {
      max-height: 40px;
    }
    .top-banner { background: #eee; text-align: center; padding: 20px 0; }
    .main-banner { background: #fff; text-align: center; padding: 20px 0; margin-bottom: 1rem; }
    .map-section { background: #fff; padding: 1rem; margin-top: 1rem; }
    footer {
      background-color: #f8f9fa;
      padding: 1rem;
      text-align: center;
      margin-top: 2rem;
    }
  </style>
</head>
<body>
  <!-- NAVBAR -->
  <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
    <div class="container">
      <a class="navbar-brand" href="/">
        <img src="https://placehold.co/80x40?text=Logo" alt="Logo">
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
              aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item mx-2"><a class="nav-link" href="/">Trang chủ</a></li>
          <li class="nav-item mx-2"><a class="nav-link" href="#">Tìm kiếm</a></li>
          <li class="nav-item mx-2"><a class="nav-link" href="#">Tin tức</a></li>
          <li class="nav-item mx-2"><a class="nav-link" href="#">Hỗ trợ</a></li>
        </ul>
      </div>
    </div>
  </nav>
  
  <!-- TOP BANNER -->
  <div class="top-banner">
    <div class="container">
      <img src="https://placehold.co/1000x100?text=Top+Advertisement+Banner" alt="Advertisement" class="img-fluid">
    </div>
  </div>
  
  <!-- MAIN BANNER -->
  <div class="main-banner">
    <div class="container">
      <img src="https://placehold.co/1000x200/ffa500/000?text=Khuyến+mãi+50,000" alt="Khuyến mãi 50,000" class="img-fluid">
    </div>
  </div>

  <!-- Nội dung trang sẽ được render tại đây -->
  <div class="container">
  <!-- Hiển thị flash message nếu có -->
  <% if flash[:notice] %>
    <div class="alert alert-success">
      <%= flash[:notice] %>
    </div>
  <% elsif flash[:alert] %>
    <div class="alert alert-danger">
      <%= flash[:alert] %>
    </div>
  <% end %>

  <%= yield %>
 
  </div>

  <!-- FOOTER -->
  <footer>
    <div class="container">
      <p class="mb-0">&copy; 2025 Demo Smocca-like. All rights reserved.</p>
      <small>Thiết kế UI mô phỏng Smocca - code ví dụ</small>
    </div>
  </footer>

  <!-- Bootstrap JS (CDN) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
